using Gtk 4.0;
using Adw 1;

Adw.Window window {
    title: "Hello, Gtk + Libadwaita!";

    Gtk.Box {
        orientation: vertical;
        
        margin-bottom: 5;
        margin-top: 5;
        margin-end: 5;
        margin-start: 5;

        Gtk.Label lbl {
            label: "arstarstaadawfdarstdv";
        }

        Gtk.Button btn {
            label: "arstart";
        }
    }
}